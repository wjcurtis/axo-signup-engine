
services:

  - type: web

    name: axo-referral-engine

    env: python

    plan: free

    buildCommand: pip install -r requirements.txt

    startCommand: gunicorn -w 2 -k gthread -b 0.0.0.0:$PORT main:app

    autoDeploy: true

    healthCheckPath: /

